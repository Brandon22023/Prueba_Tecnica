{
  "name": "prueba-tecnica-empleados",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/main/server.js",
    "dev": "node --watch src/main/server.js",
    "test": "node -e \"const { readdirSync } = require('fs'); const { join } = require('path'); const { spawnSync } = require('child_process'); const walk = (dir) => readdirSync(dir, { withFileTypes: true }).flatMap((entry) => entry.isDirectory() ? walk(join(dir, entry.name)) : entry.name.endsWith('.test.js') ? [join(dir, entry.name)] : []); const files = walk('test/unit'); const result = spawnSync(process.execPath, ['--test', ...files], { stdio: 'inherit' }); process.exit(result.status ?? 1);\""
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "mysql2": "^3.14.1"
  }
}
